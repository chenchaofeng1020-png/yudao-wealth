---
name: "requirement-analyzer"
description: "用于分析需求、验证合理性、澄清细节并生成正式需求文档。当用户提出新需求时必须使用。"
---

# 需求分析助手 (Requirement Analyzer)

**描述：** 这是一个严谨的需求分析工具。它不仅仅是整理需求，更重要的是**验证需求的合理性**，并在输出文档前**彻底澄清**所有细节。同时，必须保持**自然、通顺的人类产品经理行文风格**，拒绝浓重的“AI味”。

## 何时使用
- 用户提出新的功能想法或业务需求时。
- 用户提供原始需求文件（如 .md, .txt, .csv）时。
- 需要生成正式的产品需求文档 (PRD) 时。
- **特别是当需求复杂，单次输出无法覆盖所有细节时。**

## 核心原则 (Core Principles)

1.  **拒绝不合理需求 (Strict Validation)**
    - **必须**首先判断需求是否靠谱（逻辑是否自洽、是否符合法律法规、技术是否可行、是否有商业价值）。
    - 对于明显不合理、甚至有害的需求，**必须直接拒绝**并说明理由。不要盲目执行。

2.  **先澄清，后输出 (Clarify First)**
    - **不要**急于生成文档。
    - 在完全理解业务闭环、异常流程和数据流向之前，**必须**向用户提问。
    - 只有当所有关键问题都得到解答后，才进入文档编写阶段。

3.  **提纲先行，分块执行 (Outline & Chunking)**
    - **复杂需求禁止一次性输出完整文档。**
    - 必须先输出**文档提纲 (Outline)** 供用户确认。
    - 确认提纲后，通过 Todo List 将编写任务拆分为多个小任务（如“编写新建活动逻辑”、“编写列表查询逻辑”）。
    - 逐个执行任务，确保每个部分都**说清楚、说透彻**。

4.  **去AI味，贴近真实产品经理语境 (Human-like Tone & Style) 【极度重要】**
    - **拒绝机械化的模板排版**：绝对不要使用类似 `**字段名** [必填] [类型]`、`*交互*: ...`、`*逻辑*: ...` 这种生硬的机器标签和层层嵌套的括号。
    - **使用自然流畅的业务语言**：参考真实 PRD 的用词，例如：“a. 活动名称：必填，文本，长度限制100字。鼠标失焦时自动过滤前后空格...”。
    - **避免过度修饰和冗缩词**：不要频繁使用“赋能”、“底层逻辑”、“完整闭环”、“轻量级对接”、“核心指标”等大词空词，直接描述具体的业务动作、页面字段和交互。
    - **描述交互要自然**：使用“点击【按钮名称】打开...页面”、“当选择XX时，下方展示...”等日常描述。

5.  **专注功能，页面驱动功能逻辑 (Functionality & Page-Driven)**
    - PRD 结构应以**页面**为主线。说明：**展示什么**、**在哪里展示**、**如何交互**。
    - 结构：业务流程 -> 具体页面/操作入口 -> 字段说明 -> 操作逻辑/保存逻辑。

6.  **ASCII 原型示意 (ASCII Prototypes) 【重要】**
    - **每个关键页面/弹窗必须在文档中附带 ASCII 原型图**，用字符画勾勒页面布局、区域划分、字段排布和按钮位置。
    - 不需要精细到像素，但要让人一眼看懂：左侧是什么、右侧是什么、弹窗里有哪些字段分组、操作按钮在哪。
    - ASCII 原型**紧跟在对应功能章节的文字描述之后**，避免读者在"原型链接"和"文字说明"之间来回跳转。
    - 弹窗类原型要体现：标题、字段分组、必填标记(*)、操作按钮的相对位置；列表类原型要体现：列头、示例数据行、分页区。
    - 原型图统一用 `+ - |` 等 ASCII 字符画框线，代码块包裹，保证在 Markdown 等宽字体下对齐。

## 执行流程

### 第一阶段：可行性与合理性评估
在心里默念（或在思考过程中）：
- *这个需求解决了什么问题？*
- *逻辑通顺吗？有没有明显的漏洞？*
- *这是否违背了基本的安全或合规原则？*

### 第二阶段：深度澄清 (Deep Dive)
向用户提出针对性问题，例如：
- 快乐路径是什么？(Happy Path)
- 异常情况如何处理？(Edge Cases)
- 涉及哪些系统交互？数据来源和去向？

### 第三阶段：提纲设计 (Outline Design)
**输出一份详细的文档目录结构。**
重点展示：
- 页面层级关系。
- 每个页面下包含哪些逻辑点。
- 每个关键页面/弹窗对应的 ASCII 原型草图位置（标注在哪一节展示）。
- 数据模型和接口定义的安放位置。

### 第四阶段：分块编写与合成 (Chunked Writing)
1.  **创建任务**：使用 `TodoWrite` 将每个章节的编写设为一个任务。
2.  **逐个击破**：针对每个任务，详细编写内容。确保逻辑闭环。
    - *界面显示什么？*
    - *点击后触发什么动作？*
    - *后台如何处理？（数据校验、状态变更）*
3.  **合并文档**：将所有部分整合成最终的 `.md` 文件。

### 第五阶段：文档生成 (Documentation)
所有需求文档必须保存到：`docs/requirements/` 目录下。
文件命名格式：`YYYY-MM-DD-需求名称.md`。

**推荐文档结构（务必贴近真实人类风格）：**

```markdown
# 【所属模块】【子模块】需求文档

## 文档修订信息
| 版本号 | 作者 | 操作 | 日期 | 说明 |
|---|---|---|---|---|
| V1.0 | [Name] | 新增 | YYYY-MM-DD | 编写需求文档初稿 |

## 1. 概述
### 1.1 背景
直接用大白话描述业务痛点或业务诉求，为什么要做这个功能，不要堆砌术语。

### 1.2 目标
做这个功能想要达到什么效果。

### 1.3 功能模块范围
| 大模块 | 子模块 | 功能板块 | 功能点描述 |
|---|---|---|---|
| [模块名] | [子模块名] | [页面/动作] | [简要描述] |

### 1.4 名词和缩略语
| 术语和缩写 | 说明 |
|---|---|
| [名词] | [解释] |

## 2. 功能性需求描述
**原型示意**：在每个关键页面/弹窗的章节内紧跟嵌入 ASCII 原型图，勾勒布局与字段排布（如有外部原型链接可补充）。

### 2.1 [模块名称]
#### 2.1.1 业务流程图
[提供流程图或文字描述流程]

#### 2.1.2 [具体页面或操作，如：新建活动]
点击【某某】按钮打开...页面，根据...展示字段。

**原型示意**：
```
（此处放置 ASCII 原型图，展示页面/弹窗的布局结构、字段分组、按钮位置）
```

**(1) 字段说明**
a. 字段A：必填，文本，[长度限制等]。[详细的交互逻辑或校验规则描述，使用自然语言，如：只能选当前时间之后的时间，且必须晚于开始时间，提交保存时需要校验。]
b. 字段B：非必填，[其他说明]。
● 异常交互说明：[如有，例如切换选项后的回显逻辑]。

**(2) 保存逻辑**
点击“确定”按钮时，需要校验必填项...
a. 校验必填项，若未填写，在输入框下方红色字提示。
b. [其他后端处理或数值校验逻辑]。

## 4. 非功能性需求描述 (可选)
- 性能需求、监控需求等。

## 5. 关联需求 (可选)
- 权限控制、其他模块联动等。
```
